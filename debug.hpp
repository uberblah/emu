#ifndef EMU_DEBUG_HPP
#define EMU_DEBUG_HPP

#include "util.hpp"

namespace emu
{

    emu_uchar*  prt_rv     (emu_uchar*,emu_uchar*,void*);
    emu_uchar*  prt_ir     (emu_uchar*,emu_uchar*,void*);
    emu_uchar*  prt_rr     (emu_uchar*,emu_uchar*,void*);

}

#endif
